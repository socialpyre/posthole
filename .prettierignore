# Lockfiles — managed by their tools, never hand-formatted.
pnpm-lock.yaml
uv.lock

# Generated assets (rebuilt by `make assets`).
src/postpit/static/app.js
src/postpit/static/app.js.map
src/postpit/static/app.css

# Build / cache directories.
node_modules/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
.ty_cache/
.mypy_cache/

# Jinja templates — Prettier has no Jinja-aware formatter and would mangle
# `{% %}` / `{{ }}` tags. Keep editor formatting off for these (see .vscode/settings.json).
src/postpit/templates/

# Schema-driven env config — varlock owns the format.
.env.schema
.env.local

# Vendored / external content.
LICENSE
CHANGELOG.md
